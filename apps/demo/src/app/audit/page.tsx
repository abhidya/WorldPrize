import { WorldPrizeAudit } from '@/components/WorldPrizeAudit';

export const metadata = {
  title: 'WorldPrize Audit',
  description: 'Masked public audit trail for WorldPrize',
};

export default function AuditPage() {
  return <WorldPrizeAudit />;
}
