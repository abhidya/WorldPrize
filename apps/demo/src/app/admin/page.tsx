import { WorldPrizeAdmin } from '@/components/WorldPrizeAdmin';

export const metadata = {
  title: 'WorldPrize Admin',
  description: 'Admin dashboard for WorldPrize promotion health and stats',
};

export default function AdminPage() {
  return <WorldPrizeAdmin />;
}
